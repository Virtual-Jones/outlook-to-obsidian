{
  "permissions": {
    "allow": [
      "WebFetch(domain:outlook.cloud.microsoft)",
      "Bash(npx --yes office-addin-manifest validate manifest.xml)",
      "PowerShell(npx --yes office-addin-manifest validate manifest.xml 2>&1)",
      "PowerShell([guid]::NewGuid\\(\\).ToString\\(\\))",
      "PowerShell(New-Item -ItemType Directory -Force \"$env:APPDATA\\\\npm\" | Out-Null; npx --yes office-addin-manifest validate manifest.xml 2>&1)"
    ]
  }
}
